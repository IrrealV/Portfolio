---
interface Props {
  title: string;
  id?: string;
}

const { title, id } = Astro.props;
---

<section id={id} class="py-16 first:pt-0">
  <h2 class="text-2xl font-bold mb-8 text-(--color-text)">
    {title}
  </h2>
  <slot />
</section>
